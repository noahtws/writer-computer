# Triage

## Infra

- Extensions

## Settings

- Add a setting for the indentation level for checkboxes, <ul>, <li>, sidebar, etc., I prefer when the difference between sub-levels is more pronounced.
- Add a setting to change the font size of the editor/code blocks, it might just be me, but it feels bigger than the regular text.
- Use slider for font sizes instead of inputs
- Add a setting to change the opacity of the dividers, they feel a bit too dark right now.
- The font settings are cool, but UX-wise a slider or similar control would be more approachable for non-tech users.

## Top menu

- When clicking the "Writer" tab in the top-left corner on macOS, there's a button to install the writer command in the PATH, this might be confusing for a lot of people.
- On that same tab, there's no link to the settings, and the settings aren't obvious in the UI either. If I hadn't used ⌘P by reflex, I wouldn't have known there were settings at all.

## Sidebar

- there's no watchers in the sidebar, adding or removing files should update it
- In the settings, "Restore Workspace" is under the "Window" section, it would probably feel more intuitive under "Workspace" just above it.
- Switching workspaces in the bottom-left opens a new instance of the app. Would be nice to have a setting to control that behavior.
- File names in the sidebar are derived from the H1 in the document, smart feature, but it can be a bit disorienting when the filename and H1 don't match and you struggle to find what you're looking for. Maybe worth making this optional in settings.

  ## Editor
  - cmd+f needs work
    - isn't highlighting words properly
    - cmd+g doesn't work
  - Add an option to create a new page/file from highlighted text
  - When searching with ⌘F, the UI scrolls past the selected item, so it ends up off-screen and you have to scroll back. Could also be nice to have a visual indicator along the scrollbar showing where results are, like in some IDEs.
  - In code blocks, I'd love more padding or the ability to customize it, the rest of the UI feels spacious and premium, but in code blocks the text sits really close to the edges.
  - improve tables design
  - make mermaid charts render in fixed size widgets
  - slash menu
  - tooltip menu

## OS

- Add file associations
